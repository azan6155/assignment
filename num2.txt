#include <stdio.h>
 main() {
 char operator

printf("Enter an operator from the folowing (+ , _ , * , /) ")
scanf("%c" , operator);

printf("Enter two numbers")
scanf(%d1 %d2, &num1 , &num2);

switch(operator)
  case '+':
    result= num1 + num2
    break;
  case '-':
    result= num1-num2
    break;
  case '*':
    result=num1*num2
  case '/':
    result=num1/num2
  if num2==0
printf("0 is not allowed on / operator")
  else
printf("incorrect operator or number)

}
 return 0;
